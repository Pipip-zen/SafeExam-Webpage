function LoginPage() {
  return (
    <main className="page-shell">
      <section className="page-card">
        <span className="badge text-bg-primary mb-3">Login</span>
        <h1 className="h3 mb-3">Halaman Login</h1>
        <p className="text-secondary mb-0">
          Routing React Router sudah aktif. Form login akan dibuat di task
          berikutnya.
        </p>
      </section>
    </main>
  );
}

export default LoginPage;
